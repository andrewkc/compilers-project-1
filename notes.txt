- ahora se implementa la version con typechecker estático
- estructura del code en mi cuarderno

------------------------------------------------------

1. se agrega a reserved los tokens true, false, and, or
2. se agrega los tokens al array token_names
3. como se modifica la gramatica, se debe agregar otro método para parsear
  - modificar posiblementes tambien las llamadas
  - agregar codigo en las funciones parsers
4. agregar los operadores a la clase enum BinaryOp
5. agregar los operadores faltantes a binopToString
6. modificar el typechecker en BinaryExp
                interpreter